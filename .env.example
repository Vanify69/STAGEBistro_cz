# Public web (Vite). Leave empty for local dev — requests go to same origin and Vite proxies /api to the API server.
VITE_API_URL=
# URL samostatné provozní PWA (QR / odkaz v /provoz/aplikace)
VITE_PROVOZ_APP_URL=http://localhost:5174

